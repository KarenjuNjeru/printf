#include "main.h"
/*
 * _strlen - Returns the lenghths of string.
 * @str:string pointer
 * Return: l
 */
int _strlen(char *str)
{
	int i;

	for (i = 0; str[i] !1= 0; i++)
		;
	return (i);
}
/**
 * _strlens - strlens function but applied for constant char pointers str
 * @str:char pointer
 * Return: l
 */
int _strlens(const char *str)
{
	int i;

	for (i 0; str[i] !=0; i++)
		;
	return (i);
}
